# Root development environment. Per-service pyproject.toml files are added by
# the phase that owns each service; this file is what a fresh clone installs to
# run the task runner and the phase gates.
#
#   python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

# --- phase 0: the gates need only this -------------------------------------
pyyaml>=6.0.1

# --- phase 1: datagen ------------------------------------------------------
polars>=1.0
duckdb>=1.1
pyarrow>=17.0
numpy>=2.0

# --- phase 3+: detector ----------------------------------------------------
scikit-learn>=1.5
networkx>=3.3
shap>=0.46
jsonschema>=4.23
# torch is intentionally NOT pinned here: the CUDA build must be installed for
# the platform's driver (see docs/RUNBOOK.md). The CPU path must also work.

# --- phase 8: api ----------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
sqlalchemy>=2.0
asyncpg>=0.30
alembic>=1.13
python-jose[cryptography]>=3.3
httpx>=0.27

# --- tooling ---------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.7
